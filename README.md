# ObjectReconstructionProject
This repository implements a 3D reconstruction pipeline using COLMAP for Structure-from-Motion (SfM) and Poisson surface reconstruction for generating meshes from sparse point clouds. The pipeline also includes mesh cleaning and smoothing using Open3D and PyMeshLab.
